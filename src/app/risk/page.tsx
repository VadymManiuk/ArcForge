import type { Metadata } from "next";
import { ShieldAlert, ShieldCheck } from "lucide-react";
import { Badge, PageIntro, Panel, Progress, WarningBox } from "@/components/ui";
export const metadata:Metadata={title:"Risk Methodology"};
const inputs=[["Fixed supply",15,"No callable post-deployment mint function"],["Standard token template",15,"Known ArcForge source and bytecode assumptions"],["No blacklist",15,"No address-level transfer restrictions"],["No hidden mint",15,"Supply is fully created in the constructor"],["Creator allocation below 10%",10,"Initial allocation disclosed and below threshold"],["Creator socials present",5,"At least one attributable creator channel"],["Verified / known template",10,"Contract verification or known deployed template"],["Low holder concentration",10,"Top holder concentration below methodology threshold"],["Previous clean creator launches",5,"At least one prior launch without flags"]];
const tiers=[["Clean","80–100","good"],["Moderate","60–79","warn"],["High risk","40–59","bad"],["Extreme risk","0–39","bad"]] as const;
export default function RiskPage(){return <><PageIntro eyebrow="Risk methodology" title="Signals, not guarantees" body="ArcForge assigns a 0–100 informational score from inspectable contract properties, allocation data, holder distribution, social completeness, and creator history."/><div className="container-shell grid gap-8 pb-20 lg:grid-cols-[1fr_360px]"><div className="grid gap-3">{inputs.map(([label,points,detail])=><Panel key={String(label)} className="flex items-center gap-4 p-4"><div className="grid size-10 shrink-0 place-items-center rounded-xl bg-cyan/10 font-mono text-xs font-semibold text-cyan">+{points}</div><div><p className="text-sm font-medium text-white">{label}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></div></Panel>)}</div><aside className="grid h-fit gap-5 lg:sticky lg:top-24"><Panel className="p-5"><p className="eyebrow">Score tiers</p><div className="mt-5 grid gap-4">{tiers.map(([label,range,tone],i)=><div key={label}><div className="mb-2 flex items-center justify-between"><Badge tone={tone}>{label}</Badge><span className="font-mono text-xs text-slate-500">{range}</span></div><Progress value={[90,70,50,25][i]}/></div>)}</div></Panel><Panel className="p-5"><ShieldCheck className="size-6 text-cyan"/><h3 className="mt-4 font-semibold text-white">What the score does</h3><p className="mt-2 text-xs leading-5 text-slate-500">It organizes observable signals into a consistent comparison layer and highlights missing information.</p><ShieldAlert className="mt-6 size-6 text-amber-300"/><h3 className="mt-4 font-semibold text-white">What it does not do</h3><p className="mt-2 text-xs leading-5 text-slate-500">It cannot predict price, offchain behavior, private key compromise, social engineering, or every smart contract vulnerability.</p></Panel><WarningBox>Risk labels are informational and do not guarantee safety. ArcForge does not provide investment advice. All token launches are risky.</WarningBox></aside></div></>}
